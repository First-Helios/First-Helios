# Copy to .env and adjust for local dev. Never commit the real .env.

# Postgres connection. docker-compose's default credentials match this.
DATABASE_URL=postgresql+psycopg://helios:helios@localhost:5432/helios

# docker-compose reads these to configure the postgres service itself.
POSTGRES_USER=helios
POSTGRES_PASSWORD=helios
POSTGRES_DB=helios
