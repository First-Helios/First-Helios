[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: pure logic, no DB, no external calls
    integration: uses in-memory SQLite, mocks external dependencies
    pipeline: reserved for pipeline tracing package (not yet implemented)
