{
  "schema_version": "expectation_registry.v1",
  "description": "External expectation registry for replay quality checks. These entries describe what a published source claims should exist on a brand's first-party site. They are used only to classify replay coverage as found, missed, or not_testable and MUST NEVER be treated as first-party evidence.",
  "policy": {
    "scope": "quality_check",
    "comparison_target": "first-party replay bundles",
    "never_evidence": true,
    "max_age_days": 90
  },
  "expectations": []
}