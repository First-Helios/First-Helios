| **JSONB payload** | SpiritPool payloads must accept unknown fields from future eras. JSONB stores them without schema changes. |

If we are trusting the future we need a way to stay safe from bad attacks, this process needs to be more throughtly understood